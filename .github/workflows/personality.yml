name: encounter

on:
  push:

jobs:
  determination:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        notion: ["declare", "avail", "harmony", "superior", "lodge"]
        ratio: ["objection", "harm", "regarding", "focus"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar lA.jar
